#cdef Sum(x, y) ((x) + (y))

struct MyStruct{
}

fun main() <noret> {
	let x <auto> = MyStruct{};
	let y <MyStruct> = MyStruct{};
	let b <auto> = 10;
	if (typeof(y) == typeof(x)){
		printf("True");
	}
	printf("%d", typeof(b));
}
